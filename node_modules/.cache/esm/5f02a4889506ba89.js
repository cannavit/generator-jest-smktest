let inquirer,figlet;_950‍.x([["cliKubernetes",()=>cliKubernetes]]);_950‍.w("inquirer",[["default",["inquirer"],function(v){inquirer=v}]]);_950‍.w("figlet",[["default",["figlet"],function(v){figlet=v}]]);


async function prompt(options) {
  console.log('@1Marker-No:_354467327');

  const questions = [];

  if (!options.namespace) {
    questions.push({
      type: 'input',
      name: 'namespace',
      message: 'Add kubernetes namespace:',
    });
  }

  const answers = await inquirer.prompt(questions);

  return {
    ...options,
    namespace: options.namespace || answers.namespace,
  };

  return options;
}

       async function cliKubernetes(options) {
  console.log('@1Marker-No:_354467327');
  console.log('>>>>>-1390624476>>>>>');
  // Console kubernetes options
  options = await prompt(options);
  _950‍.g.console.log(options);
  console.log('<<<<<<<<<<<<<<<<<<<');
}
